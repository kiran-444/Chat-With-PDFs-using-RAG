# ── Web framework ─────────────────────────────────────────────────────────────
flask>=3.0.0
werkzeug>=3.0.0

# ── LangChain core + loaders ──────────────────────────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0

# ── LLM providers (choose what you need; all three kept for flexibility) ───────
langchain-groq>=0.1.0
langchain-google-genai>=1.0.0
langchain-openai>=0.1.0

# ── PDF loading ───────────────────────────────────────────────────────────────
pymupdf>=1.24.0          # PyMuPDFLoader used in app.py

# ── Embeddings ────────────────────────────────────────────────────────────────
sentence-transformers>=3.0.0   # all-MiniLM-L6-v2

# ── Vector store ──────────────────────────────────────────────────────────────
chromadb>=0.5.0

# ── Numeric / ML (chromadb + sentence-transformers dependency) ────────────────
scikit-learn>=1.3.0
numpy>=1.24.0

python-dotenv>=1.0.0